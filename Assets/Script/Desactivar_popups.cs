using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Desactivar_popups : MonoBehaviour{
    // Start is called before the first frame update

    //public GameObject pop;
   
    void Start(){

        //pop.setActive(false);
    }

    // Update is called once per frame
    void Update(){
        
    }

    public void activar_b0(){
    	//if(d_m0) pop0.setActive(false);
    	//else pop0.setActive(true);
    }
}
